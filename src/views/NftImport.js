import React from 'react'
import { StyleSheet } from 'react-native'
import { NativeBaseProvider, ScrollView } from 'native-base'

const NftImport = () => {
  return (
    <NativeBaseProvider>
      <ScrollView bg='#000'></ScrollView>
    </NativeBaseProvider>
  )
}

export default NftImport
