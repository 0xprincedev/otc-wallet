import React from 'react'
import { NativeBaseProvider, ScrollView } from 'native-base'

const Receive = () => {
  return (
    <NativeBaseProvider>
      <ScrollView></ScrollView>
    </NativeBaseProvider>
  )
}
